 # Search a range using Binary Searach
 - Range exist
 - Search left
 - Search right
   - If :Keep track whether left > right => return right
   - Else: if (nums[mid] >= target) pruning (find the least number not equal target)
